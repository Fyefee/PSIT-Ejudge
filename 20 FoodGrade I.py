"""FoodGrade I"""
def main():
    """FoodGrade I"""
    result = 0
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)
    result = count(result)

    print(result)

def count(result):
    """count"""
    weight = int(input())
    if weight >= 50 and weight <= 70:
        result += 1
    return result
main()
